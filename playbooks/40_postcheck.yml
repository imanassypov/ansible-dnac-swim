---
- name: Post-activation verification
  hosts: catc
  connection: local
  gather_facts: false
  vars_files:
    - ../vars/images.yml
  vars:
    run_id: "{{ lookup('pipe', 'date +%Y%m%d-%H%M%S') }}"
    # Derive compliance sites from golden_tag_images — the same sites where the
    # image was tagged golden are the right sites to verify after activation.
    compliance_sites: >-
      {{ swim_details.golden_tag_images
         | map(attribute='tagging_details.site_name')
         | unique | list }}
  tasks:
    - name: Run image compliance on activation sites
      cisco.catalystcenter.network_compliance_workflow_manager:
        catalystcenter_host: "{{ catalystcenter_host }}"
        catalystcenter_username: "{{ catalystcenter_username }}"
        catalystcenter_password: "{{ catalystcenter_password }}"
        catalystcenter_verify: "{{ catalystcenter_verify }}"
        catalystcenter_port: "{{ catalystcenter_port }}"
        catalystcenter_version: "{{ catalystcenter_version }}"
        catalystcenter_log: "{{ catalystcenter_log }}"
        catalystcenter_log_level: "{{ catalystcenter_log_level }}"
        catalystcenter_log_file_path: "{{ catalystcenter_log_file_path }}"
        catalystcenter_log_append: "{{ catalystcenter_log_append }}"
        validate_response_schema: "{{ validate_response_schema }}"
        catalystcenter_api_task_timeout: "{{ catalystcenter_api_task_timeout }}"
        catalystcenter_task_poll_interval: "{{ catalystcenter_task_poll_interval }}"
        state: merged
        config:
          - site_name: "{{ item }}"
            run_compliance: true
            run_compliance_categories:
              - IMAGE
            sync_device_config: false
      loop: "{{ compliance_sites }}"
      loop_control:
        label: "{{ item }}"
      register: post_compliance

    - name: Ensure compliance workflow execution succeeded
      ansible.builtin.assert:
        that:
          - post_compliance is succeeded
        fail_msg: "Image compliance workflow call failed for one or more sites."

    - name: Write post-check evidence
      ansible.builtin.copy:
        dest: "{{ playbook_dir }}/../logs/{{ run_id }}-40_postcheck.json"
        content: "{{ post_compliance | to_nice_json }}"
        mode: "0640"
